/**
 * 真机自动填入的后端地址模板。
 * 复制为本目录下的 server.local.js 并改成你的 Mac 局域网 IP（与 backend/.env 的 PUBLIC_BASE_URL 一致）。
 * server.local.js 已加入 .gitignore，不会上传到 Git。
 */
export const AUTO_SERVER_URL = 'http://192.168.43.102:8010'
